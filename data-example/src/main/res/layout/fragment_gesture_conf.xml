<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/gestures_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="@dimen/view_holder_padding"
            android:paddingEnd="@dimen/view_holder_padding"/>

        <Button
            android:layout_marginTop="32dp"
            android:id="@+id/enable_all_button"
            style="@android:style/Widget.Material.Button.Borderless.Colored"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/view_holder_padding"
            android:paddingEnd="@dimen/view_holder_padding"
            android:text="@string/gesture_config_enable_all"/>

        <Button
            android:id="@+id/disable_all_button"
            style="@android:style/Widget.Material.Button.Borderless.Colored"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/view_holder_padding"
            android:paddingEnd="@dimen/view_holder_padding"
            android:text="@string/gesture_config_disable_all"/>

    </LinearLayout>
</ScrollView>